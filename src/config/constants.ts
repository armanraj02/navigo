export const PLATFORM_VERSION = "1.0.0";
export const DEFAULTS = {
  theme: "dark" as const,
  language: "en" as const,
  timezone: "UTC" as const,
};

export const STORAGE_KEYS = {
  theme: "navigo_theme",
  settings: "navigo_settings",
};
